# 安全边界

原始 GIS 数据应视为只读。输入检查和工作流使用路径隔离及链接检查；Codex 执行前必须检查配置中的全部输入、输出路径。辅助脚本并非统一沙箱，不能向不可信用户开放任意脚本执行接口。

安装包使用 SHA256 检查文件一致性，不能证明发布者身份。安装目标拒绝重解析路径；升级保留旧版本备份，卸载通过改名保留副本。

GIS 数据处理不需要联网。依赖安装需要联网并可能执行第三方安装代码。底层 GDAL/ArcPy 的恶意数据漏洞、资源耗尽和数据文件内部的隐私信息不在完整防护范围内。

交付打包使用 delivery 模式进行文本路径脱敏，但 GIS 数据和二进制文件仍可能含敏感信息，发布前需检查。没有签名发布、操作系统级隔离或统一资源配额。
